# 戦国AIエージェントTCG — 引き継ぎ書（Claude Code が新セッションで最初に読む）

令和から戦国に転生した軍師となり、**表＝ポケポケ風カードバトル（将軍）／裏＝AIエージェントへの命令（軍師）** を1画面で同時に回すカードゲーム。
いまは **テストプレイできるMVP** が動いており、これから **細部を作り込むフェーズ**。作者の窪田さん（非エンジニア）がブラウザで遊んで出した感想を、コード側で直していく進め方。

## 起動と検証（まずこれ）

- 遊ぶ：`index.html` をダブルクリック（ビルド不要・オフライン可）。止まる時は `python -m http.server 8000` → `http://localhost:8000`。
- 構文：`node --check src/**/*.js`（各ファイル）。
- 自己対戦（AI対AI・クラッシュ/決着/バランス）：`node test/sim.js 30 oda takeda`（引数：戦数・自分の家・相手の家）。
- 操作テスト（jsdomで実DOM操作）：`node test/play-test.js`（25項目）。
- 実ブラウザ確認（レイアウト・見切れ・演出）：別ターミナルで `python -m http.server 8777` を立て、`node test/shot10.js`（ベンチ可視・ガタつき・スクロールを厳密計測）等。`test/shot*.js` は撮影スクリプト（スクショは OS の temp に出る）。
- **変更したら必ず sim＋play-test を回す**。レイアウトを触ったら shot10（ベンチ可視＝はみ出し0／ガタつき0px／スクロール無）を確認。

## 固定の作り方（崩さない）

- **バニラJS・クラシックscript**。ESモジュール／バンドラ／npmビルドは使わない。各ファイルは IIFE で `window.X` にぶら下げる（`window.CARDS` `window.COMMANDS` `window.DECKS` `window.Engine` `window.UI` `window.FACTIONS` `window.CATEGORY_COLOR`）。`index.html` が `<script>` で順に読む（data → engine → ui）。
- **外部アセット禁止**。効果音は Web Audio でその場合成（`src/ui/ui.js` の `sfx()`）。画像はカードの絵だけ（`art/images/*.png`＝GPT Image 2生成の浮世絵、リポジトリに同梱）。フォント/CDN/外部URLは使わない。
- **デザイン規律**：明朝・和紙地・墨罫・アクセントは朱（伝説＝金）。影/角丸/グラデは基本なし。**全テキストは箱に収め、はみ出し・意図しない折返しを禁止**（`nowrap`/clamp）。トークンは `styles/tokens.css`。
- 描画は `UI.render()` が `#app` を丸ごと再構築する方式。**盤は必ず画面内に収める**（`.playarea` はスクロールさせない＝スクロールバー出現でガタつくため）。短い画面(660px)でもベンチ・本陣が見えることを保つ。

## ファイル地図

| 場所 | 役割 |
|---|---|
| `index.html` | 読み込み順とエントリ（DOMContentLoaded→`UI.render()`） |
| `styles/tokens.css` / `styles/game.css` | デザイントークン / 盤・カード・パネル・演出CSS |
| `src/data/cards.js` | 武将（たね/侍大将/大名）・装備・采配・陣形。`window.CARDS` `window.FACTIONS` |
| `src/data/commands.js` | 命令札11種。`window.COMMANDS` `window.CATEGORY_COLOR` |
| `src/data/decks.js` | 5家のデッキ＋本陣の大名。`window.DECKS` |
| `src/engine/engine.js` | 状態・ターン進行・戦闘・出世・兵糧・命令層・勝敗・AI。`window.Engine` |
| `src/ui/ui.js` | 盤面描画・操作・サイドパネル・効果音/演出・スタート/布陣/詳細/命令メニュー。`window.UI` |
| `art/` | 画像生成プロンプト（`prompts/prompts.json`）・Codex手順・生成済PNG |
| `test/` | sim.js / play-test.js / diag-attack.js / shot*.js |
| `docs/` | 要件定義（現状仕様）・命令札図鑑（カード設定の在庫。最新値はコードが正） |

## 現在のルール（実装の正＝authoritative。図鑑/旧要件より新しい）

- **勝敗：相手の武将を3体討ち取れば勝ち**（`p.warlordsLost >= 3` で敗北）。
- **大名は各家の「本陣」に控える**（デッキ外）。**味方が2体討たれた時点で、3体目の砦として自動で前線へ出陣**。本陣にいる間は**無傷**（出てくる時は満タン）。大名を討てば3体目＝勝ち。手動出陣ボタンは無し。
- **出世**：たね（足軽/騎馬武者/槍足軽/弓兵/鉄砲足軽）→ 侍大将（明智など）まで。名持ち→名持ち（侍大将→大名）の進化は無い。デッキは「進化する種＋進化しない重い単体」で構成。
- **兵糧**（行動資源）：毎ターン在庫+2。武将へ送って蓄え、ワザで消費。**同じ武将へは1ターン1つまで**（前線で付けて下げて再付与する二重取りを封じる）。退き口（交代）は兵糧消費（種1/侍大将2/大名1・采配旗で無料）。
- **脳容量**（コンテキスト＝命令の資源・既定100）：自然回復は控えめ（+5/turn）。**命令を完遂しても戻らない**（撃ち得防止）。命令はメニューで**複数まとめて発令**でき、**自分の手番が終わるたびに**進行（数手番で完成）。
- 命令札11種：`/plan` `/code-review` `/security-review` `codex exec` `/compact` `--sandbox` `/agents` `/rewind` `/model` `/memory` `/deep-research`（総力調査・4手番の強力技）。名はアルファベットのまま＝本物のClaude Code/Codexコマンドが元ネタ。
- 5家：織田（速攻/鉄砲）・武田（騎馬/全体・最強）・上杉（狙撃/粘り）・徳川（耐久/反撃）・豊臣（兵站/物量・軍師寄り）。スタート画面で自分と相手の家を各5択。
- モード：対CPU（プレイヤーだけ軍師・CPUは兵力+20）／1人二役（ホットシート）／観戦（AI対AI）。

## 次フェーズ（細部の作り込み）バックログ・既知の粗

- **バランス**：AI自己対戦で武田・徳川が強め、**先攻有利**が出ている。各家の固有特性の作り込みと数値調整が主作業。豊臣は「軍師（脳容量）を回す上級者向け」でAI同士だと過小評価される（人が回すと強い）。
- **AIの賢さ**：CPUは素朴（安い命令を撃ち、最大ダメージで殴るだけ）。窪田さんの依頼で**後回し合意済み**。
- **采配/陣形のアイコン未生成**：`金瘡医/早馬/伝令/狼煙/総攻撃/兵糧調達/離反工作/城/平野/大雨` は `art:null`（プロンプト未作成）。生成するなら `art/prompts/prompts.json` に足して画像化。
- **射幸心は「演出だけ」採用済み**（配札フリップ・レア金光沢）。**報酬ループ/パック開封/コレクション（データ保存が要る大改修）は将来**、依頼があれば。
- **ドキュメントの鮮度**：`docs/命令札図鑑.md` はカード設定の在庫帳で、数値は最新でないことがある。**実装の正は `src/data/*.js` とゲーム内のデッキガイド/命令メニュー**。
- タイトル未確定（仮称『軍師』）。

## 窪田さんへの接し方（詳細はユーザーのグローバル `~/.claude/CLAUDE.md`）

- 必ず**ですます調**・日本語。専門用語はそのまま向けず1行で言い換える。
- 複数項目の報告・比較・手順は**普通の一時markdown**（HTMLにしない）。選ばせる時は**ボタンUI（AskUserQuestion）**で。
- 技術的な方向はこちらで決めて実装し報告。**本人にしか決められない事だけ**を質問する。
- git push・公開など外向きの操作は**依頼があった時だけ**行う。リポジトリは `github.com/taichita/sengoku-agent-tcg`（非公開）。
